# Choose ONE:
# Postgres (recommended, via docker compose):
DATABASE_URL="postgresql://trainmate:trainmate@localhost:5432/trainmate?schema=public"

# SQLite fallback (no docker required). If using SQLite, set provider to "sqlite"
# in prisma/schema.prisma and switch DATABASE_URL to:
# DATABASE_URL="file:./dev.db"

PORT=4000
